defaults:
  - _self_
hydra:
  run:  
    dir: ${log_dir}/robomimic-dsrl/${name}/${now:%Y-%m-%d}_${now:%H-%M-%S}_${seed}

name: robomimic_transport_dsrl_${now:%Y-%m-%d}_${now:%H-%M-%S}_${seed}
# NOTE: this run_dir is different from hydra's - meant to be persistent across script runs for checkpoint management.
run_dir: null # This will be manually set during training to allow for simple run resumes.
base_policy_path: ./dppo/log/robomimic-pretrain/transport/transport_pre_diffusion_mlp_ta8_td100_ddim-100steps_size2048/2025-04-21_08-40-08_47/checkpoint/state_3000.pt
offline_data_path: ./dppo/log/robomimic/transport/train.npz
dppo_path: ./dppo
normalization_path: ./dppo/log/robomimic/${env_name}/normalization.npz

use_wandb: True # Kind of legacy parameter, eventually just remove this and always use wandb.
resume: False # if true (e.g. slurm pre-emption), resume using given wandb.id
wandb:
  project: fast
  # run: ${now:%H-%M-%S}_${name}
  # name: ${name}
  group: robomimic-transport
  id: null # if resume is true, this must be provided.
  save_id_local_path: null # relative path to log_dir to save wandb run id - helpful for slurm pre-emption.

algorithm: fast # options are: dsrl_na, dsrl_sac

seed: 1
device: cuda:0
env_name: transport
log_dir: ./logs
obs_dim: 59
action_dim: 14
cond_steps: 1
act_steps: 8 # action chunk size of base policy
load_offline_data: True # if true, load offline demonstration data into replay buffer
deterministic_eval: False

total_timesteps: 200000
eval_interval: 500
num_evals: 200
save_model_interval: 10000
save_replay_buffer: True


env:
  n_envs:  4
  n_eval_envs: 25
  name: ${env_name}
  max_episode_steps: 800
  reset_at_iteration: False
  save_video: False
  best_reward_threshold_for_success: 1 
  reward_offset: 0
  wrappers:
    robomimic_lowdim:
      normalization_path: ${normalization_path}
      low_dim_keys: ['robot0_eef_pos',
                    'robot0_eef_quat',
                    'robot0_gripper_qpos',
                    "robot1_eef_pos",
                    "robot1_eef_quat",
                    "robot1_gripper_qpos",
                    'object'] 
    multi_step:
      n_obs_steps: ${cond_steps}
      n_action_steps: ${act_steps}
      max_episode_steps: ${env.max_episode_steps}
      reset_within_step: True
  observation_meta:
    robot0_eef_pos: 3
    robot0_eef_quat: 4
    robot0_gripper_qpos: 2
    robot1_eef_pos: 3
    robot1_eef_quat: 4
    robot1_gripper_qpos: 2
    object: 14

policy:
  type: residual # residual, residual_scale, residual_force, residual_scale2, residual_force2
  action_condition: True # Whether to condition policy on base action.
  shape_rewards: False # Whether to use PBRS from base policy values.
  base_gradient_steps: 0 # Number of gradient steps to take on base critic/value per update. If -1, same as utd. If 0, no updates.
  residual_mag_schedule: 50000 # Schedule for clamping residual actions.
  residual_mag: 0.2 # Max. magnitude to clamp residual predictions.
  # residual_sample_schedule: ... # Frequency of sampling residual actions during training.
  drop_velocity: True # Whether to drop velocity from observation for residual policy + base value.

train:
  tau: 0.005 # Target update rate
  actor_lr: 3e-4
  batch_size: 256
  train_freq: 1 # Update model after this many environment steps
  utd: 20 # Number of gradient steps to take per update
  use_layer_norm: True
  layer_size: 128 # Layer size for actor and critic
  num_layers: 3 # Number of layers for actor and critic
  discount: 0.95
  ent_coef: -1
  target_ent: 0.0
  init_rollout_steps: 20001 # Number of rollout steps of standard diffusion policy to collect for replay buffer before training starts
  init_rollout_noise_magnitude: 0.1 # Initial noise magnitude for rollout actions
  # action_magnitude: 1 # Maximum action magnitude in noise action space
  n_critics: 2 # Number of critic Q networks
  critic_backup_combine_type: min

base:
  tau: 0.005 # Target update rate
  lr: 3e-4
  fqe_steps: 5000
  vd_steps: 5000
  vd_samples: 16
  # lr_scheduler: cosine # Options: constant, linear, cosine
  batch_size: 256
  use_layer_norm: ${train.use_layer_norm}
  layer_size: ${train.layer_size}
  num_layers: ${train.num_layers}
  discount: 0.99
  n_critics: ${train.n_critics}
  critic_backup_combine_type: ${train.critic_backup_combine_type}
  
model:
  _target_: model.diffusion.diffusion_eval.DiffusionEval
  ft_denoising_steps: 0
  predict_epsilon: True
  denoised_clip_value: 1.0
  randn_clip_value: 3
  #
  network_path: ${base_policy_path}
  network:
    _target_: model.diffusion.mlp_diffusion.DiffusionMLP
    time_dim: 32
    mlp_dims: [2048, 2048, 2048]
    residual_style: True
    cond_dim: ${eval:'${obs_dim} * ${cond_steps}'}
    horizon_steps: ${act_steps}
    action_dim: ${action_dim}
  horizon_steps: ${act_steps}
  obs_dim: ${obs_dim}
  action_dim: ${action_dim}
  denoising_steps: 100
  device: ${device}
  use_ddim: True
  ddim_steps: 100
  controllable_noise: True

